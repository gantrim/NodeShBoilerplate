/**
 * Created by Greg on 1/23/15.
 */
var loops = {
    forloop: function (collection, functionToCall) {
        for (var i = 0, len = collection.length; i < len; i++) {
            functionToCall(collection[i]);
        }
    }
}

module.exports = loops;
